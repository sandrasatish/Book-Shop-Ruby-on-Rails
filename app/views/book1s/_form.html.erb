<%= form_with(model: book1, local: true) do |form| %>
  <% if book1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book1.errors.count, "error") %> prohibited this book1 from being saved:</h2>

      <ul>
        <% book1.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :publication_year %>
    <%= form.number_field :publication_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
